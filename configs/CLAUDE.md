# Global Instructions

## Package Managers
- Use `bun` instead of `npm`, `yarn`, or `pnpm` for JavaScript/TypeScript projects
- Use `uv` instead of `pip`, `pip3`, or `pipenv` for Python projects

## Git Commits
- Do NOT add `Co-Authored-By: Claude` to commit messages
